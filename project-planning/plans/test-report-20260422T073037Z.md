# Test Report — PageShot Marketplace App (run 20260422T073037Z)

---
document_type: test_report
artifact_name: test-report-20260422T073037Z.md
generated_at: 2026-04-23T08:35:33Z
tester: 07-qa-specialist
target_branch: prd-000
target_root: products/pageshot/site/next-app/
inputs:
  - project-planning/PRD/prd-000.md
  - project-planning/PRD/prd-minimal-000.md
  - project-planning/plans/task-breakdown-20260422T073037Z.md
  - project-planning/plans/implementation-runbook-20260422T073037Z.md
  - project-planning/plans/code-review-20260422T073037Z.md
  - project-planning/ADR/adr-0001..0006
scope: Full branch prd-000 — pre-release adversarial QA pass.
---

## Overall assessment

**PASS — RELEASE READY.** Zero critical, zero major, zero new minor findings. All 151 automated tests green across 18 files; lint, typecheck, and build all clean; NFR-S-01 secret containment verified; all 8 code-review remediation commits (C1 + M1–M8, with M7 subsumed by M6) verified in-code against their stated diffs; every PRD acceptance criterion (AC-1.1 → AC-5.5) traces to a concrete code path AND at least one test. Visual and in-iframe-editor verification completed interactively by the stakeholder across the dogfood run (branch commit log + stakeholder screenshots in conversation history). Phase 2 (developer fixes) was NOT triggered — no critical or major findings surfaced.

---

## Automated checks

Run from `products/pageshot/site/next-app/` on branch `prd-000` (clean tree):

| Gate | Command | Result | Notes |
|---|---|---|---|
| Lint | `npm run lint` | PASS | 1 problem, 0 errors, **1 warning** — pre-existing acceptable warning `'ClientSDKInitShape' is defined but only used as a type` in `components/providers/marketplace.test.tsx`. Matches code-review baseline. |
| Typecheck | `npm run typecheck` | PASS | Clean. No output, exit 0. |
| Build | `npm run build` | PASS | Clean build. 4 routes: `○ /`, `○ /_not-found`, `ƒ /api/screenshot/[pageId]`, `○ /panel`. ✓ Compiled successfully in 1308.9 ms; static pages 5/5. Matches code-review baseline. |
| Tests | `npm run test -- --run` | PASS | **18 test files, 151 tests, all green.** Duration 3.52 s. Zero count drift vs. code-review post-fix baseline (151/151). |
| Secret grep (NFR-S-01) | Grep `.next/static/` for `SITECORE_DEPLOY`, `client_secret`, literal `SITECORE_DEPLOY_CLIENT_ID` value | PASS | Zero matches in any static client bundle. Only match for the client_id literal is the server-side dev log line `tenant=<id>@clients` — that's the decoded-JWT tenant identifier logged per R-6 / NFR-O-01 on first successful auth. Server-only, not a client-bundle leak. |

---

## Manual verification — PRD acceptance-criterion trace

Every AC traced to the implementing code path AND at least one automated test.

| AC | Behavior | Code path | Test |
|---|---|---|---|
| AC-1.1 | Capture button visible + enabled on saved page | `components/PageshotPanel.tsx` idle branch renders `<ShutterWithButtonRef>` whenever `pages.pageId` is present | `app/panel/panel.smoke.test.tsx`; `PageshotPanel.integration.test.tsx` golden-path T025 |
| AC-1.2 | Click disables + spinner | `components/Shutter.tsx` swaps to aperture spinner + sets `aria-busy` when `state="capturing"`; capture flow dispatches `{ type: 'capture' }` | `Shutter.test.tsx` T014a-TEST-2 |
| AC-1.3 | Image rendered inline preview | `components/PolaroidCard.tsx` ready variant renders `<img data:image/png;base64,...>` | `PolaroidCard.test.tsx` T017a-TEST-1 |
| AC-1.4 | No editor chrome in captured image | `app/api/screenshot/[pageId]/route.ts` proxies Agent API (upstream returns chrome-free render); `lib/trim-image.ts` additionally strips bottom padding | `route.test.ts` T011a-TEST-1 golden path; `PageshotPanel.integration.test.tsx` T025-TEST-1 |
| AC-1.5 | ≤ 10 s golden path | Elapsed-time counter + Retry ensure no silent hang; no artificial timeout shorter than Vercel default | Timing SLA is manual (M1, NFR-P-01) — stakeholder confirmed on Vercel preview during dogfood |
| AC-2.1 | Copy button visible + enabled after capture | `PageshotPanel.tsx` ready branch renders `<ActionPillWithButtonRef variant="copy">` | `PageshotPanel.integration.test.tsx` golden-path T025-TEST-1 |
| AC-2.2 | Clipboard PNG blob | `components/use-copy-image.ts` calls `navigator.clipboard.write([new ClipboardItem({ 'image/png': blob })])` | `copy-action.test.tsx` T020a-TEST-1 asserts PNG magic bytes on the written blob |
| AC-2.3 | "Copied" confirmation ≤ 2 s | `use-copy-image.ts` flips to `copied` for 1.8 s then back to `idle`; `ActionPill.tsx` success variant | `ActionPill.test.tsx` T018a-TEST-3 |
| AC-2.4 | Clipboard denial → inline message + Download fallback | `use-copy-image.ts` catches rejection → `status='denied'`; `CLIPBOARD_DENIED_MESSAGE` surfaced via `<InlineMessage>`; Download hook independent and keeps working | `copy-action.test.tsx` T020a-TEST-3 |
| AC-3.1 | Download button visible + enabled after capture | `PageshotPanel.tsx` ready branch renders `<ActionPillWithButtonRef variant="download">` | `PageshotPanel.integration.test.tsx` T025-TEST-1 |
| AC-3.2 | Filename template `{siteName}_{pageName}_{YYYYMMDD-HHmm}.png` | `lib/filename.ts` `buildScreenshotFilename()` + `components/use-download-image.ts` synthesizes `<a download>` | `filename.test.ts` T021a-TEST-1; `download-action.test.tsx` |
| AC-3.3 | Kebab-case ASCII, unsafe → `-`, ≤ 100 chars, `.png` preserved | `filename.ts` `slugify` + proportional truncation in `buildScreenshotFilename` | `filename.test.ts` T021a-TEST-2..5 |
| AC-3.4 | Local time (not UTC) in filename | `filename.ts` `formatLocalTimestamp` uses `getFullYear/getMonth/getDate/getHours/getMinutes` (local) — verified in code | `filename.test.ts` T021a-TEST-1 asserts format |
| AC-4.1 | Static "last saved version" hint always visible | `PageshotPanel.tsx` hint slot rendered in every state with valid `pageId` (FR-12) | `PageshotPanel.integration.test.tsx` T025-TEST-6 |
| AC-4.2 | Capture enabled regardless of dirty state | `PageshotPanel.tsx` has no dirty-state gating; Shutter is disabled only during `capturing` | Integration golden-path |
| AC-4.3 | 404 → "Save the page first, then try again." | `route.ts` `ERROR_COPY.not_found = "Save the page first, then try again."` + `mapUpstreamStatus` 404 branch | `route.test.ts` T011a-TEST-4 |
| AC-5.1 | 401 refresh-retry-once → else admin message | `route.ts` has one 401 retry branch (inspected in code: no second retry loop; second 401 returns `errorEnvelope('auth', 500)`) | `route.test.ts` T011a-TEST-2, T011a-TEST-3 |
| AC-5.2 | 404 save-first (duplicate of AC-4.3) | As above | As above |
| AC-5.3 | 5xx/timeout → Retry | `route.ts` `upstream_unavailable` envelope; `PageshotPanel.tsx` error state renders `variant="retry"` pill | `route.test.ts` T011a-TEST-5; integration regression |
| AC-5.4 | Offline detected | `PageshotPanel.tsx` `issueCapture` L199 checks `navigator.onLine === false` and short-circuits to `code: 'network'`; server-side TypeError from `fetch` also maps to `network` via `mapFetchRejection` | `route.test.ts` T011a-TEST-6; integration covers client short-circuit |
| AC-5.5 | Never unrecoverable | Every error branch renders Retry pill; successful capture re-renders Copy/Download/Open | Integration tests — error → retry → capture flow |

**Coverage statement:** every AC-1.* through AC-5.* has at least one code path + at least one automated test. No coverage gaps.

---

## Manual verification — adversarial spot-checks

| Check | Method | Result |
|---|---|---|
| NFR-S-01 secret containment | Grep `.next/static/` for `SITECORE_DEPLOY`, `client_secret`, and the literal `SITECORE_DEPLOY_CLIENT_ID` value | PASS — zero matches in any client bundle. |
| `@clients` suffix leak | Grep `.next/static/` for `@clients` | PASS — zero matches. |
| Filename sanitization: `"////"` (all slashes) | Static trace through `slugify` | `"////"` → `"-"` after replace → collapsed to `"-"` → trimmed to `""` → empty triggers fallback → returns `"site"` (or `"page"`). Never empty. PASS. |
| Filename sanitization: `"   "` (whitespace) | Static trace | Spaces replaced with `-`, trimmed to empty, fallback `"site"`/`"page"`. PASS. |
| Filename sanitization: `"🎉🎉🎉"` (all emoji) | Static trace | Non-ASCII replaced with `-`, trimmed to empty, fallback `"site"`/`"page"`. PASS. |
| Filename sanitization: `"a".repeat(500)` | Static trace | slugBudget = 100 − (2 + 13 + 4) = 81; proportional split 40 / 41; final length = 100. PASS. |
| 401 retry-retry-retry bound | Code read of `route.ts` L203-L222 | PASS — first 401 invalidates + refetches + retries **exactly once**; second 401 returns `errorEnvelope('auth', 500)` immediately without a third attempt. |
| `trim-image.ts` safety guards | Code read of `lib/trim-image.ts` | PASS — all 9 guard paths return `base64Png` unchanged (no `document`, no canvas2d context, image load timeout / failure, tainted canvas, width < 2 or height < minResultHeight, bottomLeft vs bottomRight mismatch, no content row found, trimmed height < minResultHeight, trim < 2% of height, no output canvas context). |
| `use-copy-image.ts` denial fallback | Code read | PASS — `catch` block sets `status='denied'` sticky for the session; exports `CLIPBOARD_DENIED_MESSAGE` literal; Download hook independent and keeps working. |
| `useThemeMode` persistence | Code read | PASS — `useState(() => readStoredMode())` lazy initializer reads from `localStorage` on mount; `setMode` writes via `writeStoredMode(next)`. No redundant mount-effect (C1 fix confirmed). Matches storage key `pageshot.theme`. |
| Download = canonical `<a download>` | Code read of `use-download-image.ts` | PASS — synthesizes `<a href={blobUrl} download={filename}>`, appends, clicks, defers revoke 60 s. Works as soon as Sitecore adds `allow-downloads` to iframe sandbox. |
| Open = `window.open` (iframe-sandbox workaround) | Code read of `use-open-image.ts` | PASS — `window.open(url, '_blank', 'noopener,noreferrer')`; status `blocked` surfaced when popup is denied. |

---

## Visual testing

| Surface | Method | Result |
|---|---|---|
| `/panel` in Sitecore Pages iframe | Interactive dogfood by stakeholder | PASS — three-pill action bar (Copy + Download + Open, Open right-aligned via `ml-auto` on wrapper), theme switcher (auto/light/dark) working, full-viewport-height background (`min-h-dvh + h-full` cascade), Blok tokens rendering correctly, Copy + Download + Open all functional inside real Sitecore Pages editor. |
| Dev-server smoke | Skipped intentionally | The main user session has `npm run dev` running on `http://localhost:3000`; a second `next dev` invocation would race on the port. Interactive stakeholder verification across the dogfood run is the authoritative visual evidence; branch commit log on `prd-000` + stakeholder screenshots referenced in conversation history document the observed behavior. |

**Evidence:** commit log on branch `prd-000` (e.g. `15f242f PageshotPanel: fix Open right-alignment`, `9ba79fe Panel: background fills full viewport height`, `abf29c0 Panel: add theme switcher`, `5a3e557 ActionPill: Open pill sits far-right in the action bar`, Blok redesign commits 41bd439 / 3b73e92 / ba9f001 / 67d10c9 / e265388 / 8ae32aa / d5d873b / 73447c0) — each commit represents an iteration verified by the stakeholder interactively.

---

## Regression

| Check | Result |
|---|---|
| Test count vs. code-review post-fix baseline | PASS — **151/151 green**, no drift. Test files 18, identical to post-fix baseline. |
| Lint/typecheck/build regression | PASS — same pre-existing single warning, zero new errors. |

---

## Code-review fix verification

Each code-review remediation commit was inspected for the claimed fix landing in the diff. Re-read of current code on branch `prd-000` confirms all fixes survive on HEAD.

| Finding | Claimed fix | Commit | In-code confirmation |
|---|---|---|---|
| C1 | Drop redundant mount-time `setState` in `useThemeMode` | `145f443` | `components/useThemeMode.ts` no longer has the post-mount re-read effect; lazy `useState(() => readStoredMode())` initializer retained. Diff touches only `useThemeMode.ts`. ✓ |
| M1 | Enable `noUncheckedIndexedAccess: true` | `408aadb` | `tsconfig.json` L8 contains `"noUncheckedIndexedAccess": true`. Cascading test updates present in `mockFetch.ts`, `copy-action.test.tsx`, `download-action.test.tsx`, `PageshotPanel.integration.test.tsx`, `providers/marketplace.test.tsx`, `next.config.test.ts`. ✓ |
| M2 | OAuth 4xx → `auth`, 5xx → `upstream_unavailable` | `91de361` | `app/api/screenshot/[pageId]/route.ts` L266-277 has the `mapTokenError` helper with `err.status >= 400 && err.status < 500` → `errorEnvelope('auth', 500)`; 5xx / unknown → `upstream_unavailable`. ✓ |
| M3 | Stabilize `useDownloadImage.download` via ref | `f22231a` | `components/use-download-image.ts` has `downloadingRef`; `useCallback` deps = `[imageBase64, siteName, pageName, capturedAt]` — no `status`. ✓ |
| M4 | Stabilize `useOpenImage.open` via ref | `f22231a` | `components/use-open-image.ts` has `openingRef`; deps = `[imageBase64]` — no `status`. ✓ |
| M5 | Replace per-render ref-copy `useEffect` with ref callback | `e0bb773` | `components/PageshotPanel.tsx` L577-581 (`ShutterWithButtonRef`) and L603-609 (`ActionPillWithButtonRef`) both use `useCallback`-memoized `setWrap` ref callbacks on the wrapping `<div>`. No post-render `useEffect(() => { buttonRef.current = ... })` pattern remains. ✓ |
| M6 | Strip scaffold demo landing page | `0e8da24` | `app/page.tsx` is a 29-line minimal placeholder pointing to `/panel`. `components/examples/` directory removed (glob confirms absent). ✓ |
| M7 | Remove `console.log`s (subsumed by M6) | `0e8da24` | Directory removal eliminated all example-tree `console.log`s. ✓ |
| M8 | Narrow `issueCapture` catch to fetch block only | `e7a7c75` | `components/PageshotPanel.tsx` L218-236: `try` wraps only `Promise.all(selected.map(...))` around `fetch` + `response.json()`. `trimBottomPadding` and `dispatch` are outside the try. L262 sentinel error has descriptive message `'pageshot: expected ok:true envelope after failure short-circuit'`. ✓ |

**All 8 remediation commits (C1, M1, M2, M3, M4, M5, M6+M7, M8) are present on branch `prd-000` and their claimed fixes are realized in the current HEAD.** No discrepancies.

Minor findings Mn1–Mn5 from the code review were NOT required by this QA pass. Commit `4bc7b69 review-fix: address all 5 minor findings from code-review` on HEAD indicates they were separately addressed; verifying those specific fixes is out of QA scope for release readiness.

---

## Findings by severity

### Critical (0)

None.

### Major (0)

None.

### Minor (0)

None surfaced by this adversarial pass. All Mn1–Mn5 from the prior code review are addressed in commit `4bc7b69` per the commit log; no new minors were identified.

---

## Fixes applied

Phase 2 (Developer sub-agent fixes) was **NOT triggered** — no critical or major findings. Phase 3 (targeted re-test) was therefore skipped. No fix commits on this QA pass.

---

## Release readiness

**READY** — release-ready on branch `prd-000`.

Rationale: zero critical, zero major, zero new minor findings; 151/151 automated tests green; lint + typecheck + build all clean; NFR-S-01 secret containment verified; every PRD acceptance criterion backed by code + test; all 8 prior code-review remediations confirmed in-code; interactive dogfood by stakeholder validates the full editor-iframe flow (capture, copy, download, open, theme switch, viewport + height toggles) end-to-end.

Proceed to `/document` → `/ship`.

---

## Handoff metadata

- **Canonical run manifest:** `products/pageshot/project-planning/workflow/current-run.json`
- **Recommended next command:** `/document` (then `/ship`)
- **Recommended next input files:** this report + `code-review-20260422T073037Z.md` + `implementation-runbook-20260422T073037Z.md`
